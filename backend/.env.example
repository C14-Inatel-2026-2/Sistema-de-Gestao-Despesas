# SQLite local (padrão, não precisa configurar nada)
# DATABASE_URL=sqlite:///./despesas.db

# PostgreSQL (produção / staging)
DATABASE_URL=postgresql+psycopg2://usuario:senha@localhost:5432/despesas
